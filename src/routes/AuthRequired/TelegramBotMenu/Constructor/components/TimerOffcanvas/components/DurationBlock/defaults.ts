import type { Duration, DurationBlockFormValues } from './types';

export const defaultDuration: Duration = 30;
export const defaultDurationBlockFormValues: DurationBlockFormValues = {
  duration: defaultDuration,
};
