import type { Image, ImageBlockFormValues } from './types';

export const defaultImage: Image | null = null;
export const defaultImageBlockFormValues: ImageBlockFormValues = {
  image: defaultImage,
};
