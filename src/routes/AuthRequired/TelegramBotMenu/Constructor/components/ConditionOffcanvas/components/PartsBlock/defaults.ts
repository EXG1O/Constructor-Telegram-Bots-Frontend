import { defaultPart } from './components/PartItem/defaults';

import type { Parts, PartsBlockFormValues } from './types';

export const defaultParts: Parts = [defaultPart];
export const defaultPartsBlockFormValues: PartsBlockFormValues = {
  parts: defaultParts,
};
