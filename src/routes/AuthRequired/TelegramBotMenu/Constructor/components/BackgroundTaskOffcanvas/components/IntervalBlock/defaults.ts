import type { Interval, IntervalBlockFormValues } from './types';

export const defaultInterval: Interval = 1;
export const defaultIntervalBlockFormValues: IntervalBlockFormValues = {
  interval: defaultInterval,
};
