import { Type, type TypeBlockFormValues } from './types';

export const defaultType: Type = Type.StartCommand;
export const defaultTypeBlockFormValues: TypeBlockFormValues = {
  type: defaultType,
};
