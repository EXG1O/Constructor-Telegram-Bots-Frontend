import type { Message, MessageBlockFormValues } from './types';

export const defaultMessage: Message = { text: '' };
export const defaultMessageBlockFormValues: MessageBlockFormValues = {
  message: defaultMessage,
};
