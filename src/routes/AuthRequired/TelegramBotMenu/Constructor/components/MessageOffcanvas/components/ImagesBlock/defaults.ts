import type { Images, ImagesBlockFormValues } from './types';

export const defaultImages: Images = [];
export const defaultImagesBlockFormValues: ImagesBlockFormValues = {
  images: defaultImages,
};
