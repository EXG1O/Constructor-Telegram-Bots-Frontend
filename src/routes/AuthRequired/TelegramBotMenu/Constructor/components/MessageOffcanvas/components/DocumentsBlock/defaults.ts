import type { Documents, DocumentsBlockFormValues } from './types';

export const defaultDocuments: Documents = [];
export const defaultDocumentsBlockFormValues: DocumentsBlockFormValues = {
  documents: defaultDocuments,
};
